apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: appwrite-ingress
spec:
  rules:
    - host: appwrite.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: appwrite-service
                port:
                  number: 80
